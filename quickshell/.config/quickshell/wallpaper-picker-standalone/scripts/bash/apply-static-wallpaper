#!/bin/bash
# Apply a static image wallpaper
wall_path="$1"

if [ -z "$wall_path" ] || [ ! -f "$wall_path" ]; then
  echo "Error: invalid wallpaper path: $wall_path" >&2
  exit 1
fi

dms ipc call wallpaper set "$wall_path"
